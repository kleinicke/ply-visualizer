*.ply
!engine/examples/example-point-cloud.ply
*.xyz
*.exr
.vscode-test
node_modules
out
*.vsix
testfiles/new_formats/**
testfiles
# Root-anchored: this is for the scratch dataset cache at the repo root. An
# unanchored `dataset/` also matches src/dataset/, which is real source — new
# files there were silently unstageable while the existing ones stayed tracked.
/dataset/
coverage/
.nyc_output/

# Engine (shared viz core + standalone page) build outputs and dependencies
engine/node_modules/
engine/dist/

# Rust build output for the vendored decoder crate
crates/image-decoders/target/

# Test outputs
engine/test-results/
engine/playwright-report/

# OS files
.DS_Store

# Benchmark output (scripts/benchmark-vscode.mjs); regenerated on every run.
benchmark-vscode-result.json

# Local Netlify folder
.netlify

# Local publishing credentials (never source or release assets).
/jetbrains_token.md
/.local/
